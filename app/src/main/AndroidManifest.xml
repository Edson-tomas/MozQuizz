<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.etomas.mozquizz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QuizzCategoryActivity" />
        <activity android:name=".QuizzRankActivity" />
        <activity android:name=".AddingQuestionActivity" />
        <activity android:name=".FirstPageActivity" />
        <activity android:name=".MathematicsActivity" />
        <activity android:name=".PhysicsActivity" />
        <activity android:name=".BiologyActivity" />
        <activity android:name=".PortugueseActivity" />
        <activity android:name=".EnglishActivity" />
        <activity android:name=".ChemistryActivity" />
        <activity android:name=".ComputerScienceActivity" />
        <activity android:name=".GeographyActivity" />
        <activity android:name=".HistoryActivity"></activity>
    </application>

</manifest>